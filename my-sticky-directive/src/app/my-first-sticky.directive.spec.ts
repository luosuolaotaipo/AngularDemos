import { MyFirstStickyDirective } from './my-first-sticky.directive';

describe('MyFirstStickyDirective', () => {
  it('should create an instance', () => {
    const directive = new MyFirstStickyDirective();
    expect(directive).toBeTruthy();
  });
});

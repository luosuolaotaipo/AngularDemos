import { MyNguiStickyDirective } from './my-ngui-sticky.directive';

describe('MyNguiStickyDirective', () => {
  it('should create an instance', () => {
    const directive = new MyNguiStickyDirective();
    expect(directive).toBeTruthy();
  });
});
